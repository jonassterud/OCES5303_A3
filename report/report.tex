\documentclass[12pt,a4paper,english]{article}

\usepackage[utf8]{inputenc}
\usepackage[a4paper,hmargin=10mm,vmargin=25mm]{geometry}
\usepackage[backend=biber,style=numeric,sortcites,firstinits=true]{biblatex}
\usepackage[skip=5pt, indent=0pt]{parskip}
\usepackage[hidelinks]{hyperref}
\usepackage[english]{babel}
\usepackage[T1]{fontenc}
\usepackage{indentfirst}
\usepackage{enumitem}
\usepackage{textcomp}
\usepackage{caption}
\usepackage{url}
\usepackage{csquotes}
\usepackage{graphicx}
\usepackage{float}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}

\urlstyle{sf}
\addbibresource{./refs.bib}
\graphicspath{ {../figures/} }

\title{\textbf{OCES5303} \linebreak Extended Project}
\author{Jonas Mathisrud Sterud}

\begin{document}
    \maketitle

    \begin{abstract}
        Historically, oceanic eddies have been detected using ``Sea Level Anomolies'' (SLA) satellite images.
        However, the SLA resolution limits the accuracy - therefore, we'll deploy a ``Convolutional Neural Network'' (CNN)
        to detect eddies using other satellite formats without such limitations: ``Sea Surface Temperature'' (SST) and ``Chlorophyll-a'' (CHLA).
    \end{abstract}

    \begin{figure}[H]
        \begin{center}
            \includegraphics[width=15cm]{cover.jpg}
        \end{center}
    \end{figure}

    \newpage
    \tableofcontents
    \newpage

    \section{The Data}

    \subsection{Overview}

    We have five different folders with data, retrieved from the ``Ocean Eddy Identification'' competition \cite{data:mercatorocean}:
    \begin{description}[labelindent=1cm]
        \item[TRAIN\_png] - Dated \verb|.png| satellite training images across various categories.
        \item[TEST\_png] - Dated \verb|.png| satellite testing images across various categories.
        \item[LABELS\_TRAIN] - Dated \verb|.csv| files with training labels.
        \item[train\_images\_pkl] - Dated folders, each containing various \verb|.pkl| files for the traning dataset.
        \item[test\_image\_pkl] - Dated folders, each containing various \verb|.pkl| files for the test dataset.
    \end{description}
    
    The \verb|.pkl| files contain various useful information, such as the contours and centres of the cyclonic and anticyclonic eddy's (in the training set).
    They also contain latitude and longitude positions of the images, which we'll use later for visualization.

    Additionally, we have labels for the test dataset as a \verb|.pkl| file.

    Our image labels are one of: \verb|0|, \verb|1| or \verb|2|.

    \subsection{Parsing}

    We load, and parse, all the \verb|.pkl| files and put them into a dictionary.
    The \verb|.png| images are loaded in as grayscale images, and are then sorted under their respective dates, categories and image numbers - which are extracted from their filenames.
    
    Then, we combine all the images, and their corresponding metadata (from the \verb|.pkl| files) into a Pandas \verb|Dataframe|.

    \section{Exploratory Data Analysis}    

    \subsection{Image Categories}

    We have four different image categories, displaying different properties:
    \begin{description}[labelindent=1cm]
        \item[SST] - Temperature of the sea surface.
        \item[CHLA] - Concentration of Chlorophyll-a.
        \item[SLA] - Sea Level Anomalies (i.e. height of the sea).
        \item[EDDY] - Contours of the cyclones (as images).
    \end{description}
    
    The figures below, shows a small sample of these images, from top to bottom (SST, CHLA, SLA, EDDY), categorized into our three labels.

    \begin{minipage}{.33\textwidth}
        \begin{figure}[H]
            \begin{center}
                \includegraphics[width=.9\textwidth]{images_with_no_cyclone.png}
                \caption{No cyclonic eddies}
                \label{fig:no_cyclonic_eddies}
            \end{center}
        \end{figure}
    \end{minipage}
    \begin{minipage}{.33\textwidth}
        \begin{figure}[H]
            \begin{center}
                \includegraphics[width=.9\textwidth]{images_with_cyclone.png}
                \caption{Cyclonic eddies}
                \label{fig:cyclonic_eddies}
            \end{center}
        \end{figure}
    \end{minipage}
    \begin{minipage}{.33\textwidth}
        \begin{figure}[H]
            \begin{center}
                \includegraphics[width=.9\textwidth]{images_with_anticyclone.png}
                \caption{Anticyclonic eddies}
                \label{fig:anticyclonic_eddies}
            \end{center}
        \end{figure}
    \end{minipage}

    Since our images are from the northern hemisphere (\ref{fig:sst_map}) (\ref{fig:chla_map}), the anticyclonic eddies and cyclonic eddies
    are rotating clockwise, and counterclockwise, respectively. \cite{article:nutrition_eddies}

    In a cyclonic eddy, we would expect phytoplankton to grow in the edges of the eddy near the surface (due to the supply of nutrient-rich bottom-water), and
    therefore show up as a chlorophyll circle in our images. \cite{website:tracking_eddies}

    This does not seem to be very obvious - at least in this small subset of images.

    Furthermore, we would expect the cyclonic eddies (counterclockwise) to have a cold core and the anticyclonic eddies (clockwise) to have a warm core. \cite{article:eddy_temperature}

    \subsection{Filtering Images} \label{sec:filtering_images}

    \begin{center}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{sst_map_train.png}
                    \caption{SST images}
                    \label{fig:sst_map}
                \end{center}
            \end{figure}
        \end{minipage}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{chla_map_train.png}
                    \caption{CHLA images}
                    \label{fig:chla_map}
                \end{center}
            \end{figure}
        \end{minipage}
    \end{center}

    By plotting all the images at their corresponding positions, we see that all of our observations are in the northern hemisphere.
    Additionally, some of the images seem to be corrupt, in that they are almost only one solid color. The common factor between these images for the SST images seems to be
    that they're taken close to land - we might be getting some interference from the shore. For the CHLA images, we see that most of the images seem to be corrupted - this is most
    likely due to cloud cover.

    For SST, a good filtering strategy turned out to be mapping all of the images to a scale from negative one to positive one, and then filtering out images where the absolute pixel sums were in the top 6\% of images.
    Similary, for CHLA, mapping all of the images to a scale from zero to positive one, and filtering out images where the pixel sum were in the bottom 98\%, turned out to be a good filter, as seen below.
    
    \begin{center}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{sst_map_train_filtered_in.png}
                    \caption{SST images, filtered in}
                    \label{fig:sst_map_filtered_in}
                \end{center}
            \end{figure}
        \end{minipage}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{chla_map_train_filtered_in.png}
                    \caption{CHLA images, filtered in}
                    \label{fig:chla_map_filtered_in}
                \end{center}
            \end{figure}
        \end{minipage}
    \end{center}
    \begin{center}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{sst_map_train_filtered_out.png}
                    \caption{SST images, filtered out}
                    \label{fig:sst_map_filtered_out}
                \end{center}
            \end{figure}
        \end{minipage}
        \begin{minipage}{.49\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=\textwidth]{chla_map_train_filtered_out.png}
                    \caption{CHLA images, filtered out}
                    \label{fig:chla_map_filtered_out}
                \end{center}
            \end{figure}
        \end{minipage}
    \end{center}

    \section{Transforming Images}

    \subsection{Orientation}

    In the matrix containing per-pixel latitude coordinates for SST, it was discovered that the SST images are essentially flipped
    vertically. To resolve this, all of the SST images were flipped along their vertical axis, as illustrated below.
    This was not the case with the CHLA images.

    \begin{center}
        \begin{minipage}{.329\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=.8\textwidth]{sst_before_flip.png}
                    \caption{SST}
                    \label{fig:sst_before_flip}
                \end{center}
            \end{figure}
        \end{minipage}
        \begin{minipage}{.329\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=.8\textwidth]{sst_after_flip.png}
                    \caption{SST (flipped)}
                    \label{fig:sst_after_flip}
                \end{center}
            \end{figure}
        \end{minipage}
        \begin{minipage}{.329\textwidth}
            \begin{figure}[H]
                \begin{center}
                    \includegraphics[width=.8\textwidth]{chla_after_flip.png}
                    \caption{CHLA}
                    \label{fig:chla_after_flip}
                \end{center}
            \end{figure}
        \end{minipage}
    \end{center}

    \subsection{Scaling and Resizing}

    There's different resolutions both inter- and intra-category between SST and CHLA.
    We need to resize these images in order to make the compatible with PyTorch's tensors.

    There's various methods to do this, most of which require some form of interpolation.
    But, given the low resolution, and the potential loss of quality, this might not be a good solution.

    Considering that we seem to be classifying images where the eddies are centered in the
    image (\ref{fig:cyclonic_eddies}) (\ref{fig:anticyclonic_eddies}), and that the images are \textit{almost} the same resolution,
    cropping the edges of the images seemed to be a better solution.

    Additionaly, the images are are in greyscale, with pixel values between 0 and 255.
    We'll scale these values before feeding them into our model, using a \verb|MinMaxScaler| to a range between 0 and 1.

    \subsection{Applying Filtering}

    As showed previously (\ref{sec:filtering_images}), we have a way to filter out ``corrupt'' images.

    For SST, the reasoning for the corrupted images seems to be quite obvious, in that there's some interference from the land.
    Considering there's also so few samples, it seems quite safe to remove these from our training samples, but we'll test multipile implementations.

    However, for the CHLA images, we're filtering out approximately 92\% of the images, and removing these would most likely degrade the performance
    of our model quite severely. There's other options, such as masking out the corrupt images, e.g. by filling them with zeros.
    We could also add a new channel, which specifies if an image is corrupt or not, e.g. a channel where all the pixels are ones marks for corrupt CHLA images
    and vice-versa for zeros. These approaches are also something we could do for the SST images.

    We'll try various techniques and see which ones perform well on our data


    \section{Convolution Neural Network}

    \subsection{Advantages}

    A ``Convolution Neural Network'' (CNN) is a feedforward neural network, with the purpose of learning features from an image.
    This involves a kernel, i.e. convolution matrix, being convoluted over the image matrix. We can specify a custom kernel size
    (i.e. amount of pixels being included in each convolution) and options such as stride and padding.

    \cite{enwiki:1349825931}
    \cite{dumoulin2016guide}

    \subsection{Configuration}

    \dots


    \subsection{Model Selection}

    \dots

    \subsection{Loss}

    \section{Results}

    \dots

    


    % These images were filtered out by scaling each image using a \verb|MinMaxScaler| (with a range between negative one and positive one), and removing any images with an absolute pixel sum above
    % 95\% of our samples.

%    \tableofcontents
%    \newpage

   \newpage
   \printbibliography
\end{document}
